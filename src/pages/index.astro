---
import PageLayout from "../layouts/PageLayout.astro";
import { talks } from "../data/talks";
import PastTalk from "../components/landing/PastTalk.astro";
---

<PageLayout>
  <div
    class="prose relative mx-auto flex w-full max-w-screen-lg flex-col items-center gap-24 px-4 py-16 text-white"
  >
    <section class="relative z-50 mx-auto max-w-screen-sm">
      <h1 class="text-center text-screen font-bold text-TED-red">
        Community where<br />ideas grow
      </h1>

      <div class="">
        <div class="aspect-video w-full rounded-lg">
          <img
            src="/assets/homepage/1.png"
            alt=""
            class="object-fit h-full w-full"
          />
        </div>
      </div>
    </section>

    <section class="m relative z-50 grid grid-cols-1 gap-12 lg:grid-cols-2">
      <div class="flex flex-col gap-2">
        <h2 class="my-0 whitespace-pre-wrap text-subhead text-TED-yellow">
          What is TEDxChulalongkornU?
        </h2>
        <div class="text-body text-white">
          <p>
            In the spirit of ideas worth spreading, TED has created a program
            called TEDx. TEDx is a program of local, self-organized events that
            bring people together to share a TED-like experience. Our event is
            called TEDxChulalongkorn, where x = independently organized TED
            event.
          </p>
        </div>
      </div>

      <div class="w-full overflow-hidden">
        <div class="aspect-square w-full rounded-lg">
          <img
            src="/assets/homepage/2.png"
            alt=""
            class="h-full w-full object-contain"
          />
        </div>
      </div>
    </section>

    <section class="relative z-50 flex flex-col items-center">
      <h2 class="my-0 text-center text-screen text-TED-red">Past Talks</h2>
      <p class="text-center text-title text-white">
        All talks that we're proud to present
      </p>

      <div class="mx-2 flex w-screen overflow-x-scroll sm:px-2 md:px-4 lg:px-8">
        {talks.map((item) => <PastTalk {...item} />)}
      </div>
    </section>
  </div>
</PageLayout>
